;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname tp_racket_2) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
; = : Number -> Boolean


;(define (can-vote? age)
;  (>= age 18))
;
;(can-vote? 19)
;(string-append "alpha" "bet")
;(string-length "alphabet")
;;(define x [1 2 2])

(define (u x)
  (cond
  [(= x 1)   "UCAD is number one in Senegal"]
  [(= x 2)    "UGB is number two in Senegal"]
  [(= x 3) "UADB is number three in Senegal"]
  [else  "Others are classified"]))